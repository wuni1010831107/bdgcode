# DataDev Agent — 产品路线图

> 版本：v0.1.0  
> 更新日期：2026-05-29

---

## 愿景

成为数据工程师的首选 AI 副驾驶，覆盖数据开发全生命周期，让工程师从繁琐的编码和排错中解放出来，专注于业务价值和架构设计。

---

## 当前版本：v0.1.0（MVP）— 已完成

### 已实现能力

| 能力 | 说明 |
|-----|------|
| CLI REPL | 交互式对话界面，流式输出 |
| LLM Provider 抽象层 | 支持 Anthropic / OpenAI / 本地模型切换 |
| 任务规划（Planner） | 将用户意图分解为可执行步骤 |
| 执行器（Executor） | 执行 Planner 输出的步骤 |
| Iceberg DDL 生成 | 生成标准 Iceberg 建表 SQL，自动写入文件 |
| ClickHouse DDL 生成 | 生成 ClickHouse MergeTree 建表 SQL |
| Spark SQL 生成 | 基础 Spark SQL 模板 |
| 安全扫描 | 基于正则的敏感字段检测 + 脱敏 SQL 生成 |
| 文件工具 | 读写扫描本地文件 |
| MCP Client | MCP 协议客户端，支持外部工具扩展 |
| 自观测数据采集 | 会话/工具调用/任务完成记录 |
| Session 管理 | 对话历史持久化 |
| 知识库 | SQL 模板 + 安全规则 + 错误手册 |
| 配置管理 | YAML 配置文件 + 环境变量 |

### 已知限制

- 仅代码生成，不直接执行 Spark/Flink 任务
- 安全扫描依赖预定义正则，LLM 辅助识别尚未启用
- 知识库检索为简单文本匹配
- 无 Prompt Caching（token 成本较高）
- 无 LLM 调用重试机制

---

## v0.2.0 — 增强核心体验（预计 4-6 周）

### 目标

提升 Agent 的实际可用性和代码生成质量。

### 功能规划

| 功能 | 优先级 | 说明 |
|-----|-------|------|
| **Prompt Caching** | P0 | Anthropic SDK 缓存系统 prompt 和知识库内容，降低 50%+ token 消耗 |
| **LLM 调用重试** | P0 | 指数退避重试 + 优雅降级 |
| **Planner 能力对齐** | P0 | 移除 Planner 中 Executor 不支持的动作类型，避免用户困惑 |
| **SQL 注入防护** | P0 | 对 LLM 生成的 SQL 参数进行转义和校验 |
| **文件写入集成** | P0 | Executor 使用 FileWriter 真正写入 SQL 文件到项目目录 |
| **REPL 输入校验** | P1 | 限制单次输入长度，防止 token 溢出 |
| **错误恢复** | P1 | LLM 调用失败时提供友好提示和重试选项 |
| **上下文压缩** | P1 | 长对话自动压缩，保持核心上下文在窗口内 |

### 技术债偿还

- 修复 `KnowledgeLoader.search()` 的内存问题（增量索引）
- 修复 `FileScanner` 递归扫描的性能
- 统一所有模块的日志输出格式

---

## v0.3.0 — 直接执行能力（预计 6-8 周）

### 目标

从"代码生成"升级为"代码生成 + 执行"，真正端到端完成数据任务。

### 功能规划

| 功能 | 优先级 | 说明 |
|-----|-------|------|
| **Spark SQL 执行** | P0 | 通过 `spark-sql` CLI 子进程执行生成的 SQL，返回结果 |
| **Flink SQL 执行** | P0 | 通过 `flink run` 提交 Flink 作业，监控执行状态 |
| **ClickHouse 查询** | P0 | 通过 `clickhouse-client` 或 JDBC 执行查询 |
| **执行结果解析** | P0 | 将 SQL 执行结果格式化为表格输出 |
| **DDL 执行确认** | P1 | 执行 DDL 前二次确认，防止误操作 |
| **任务状态监控** | P1 | Flink/Spark 作业提交后持续轮询状态 |

### 新增工具

| 工具 | 说明 |
|-----|------|
| `SparkExecutor` | spark-sql 子进程管理 |
| `FlinkExecutor` | flink run 作业提交和监控 |
| `ClickHouseExecutor` | ClickHouse JDBC 查询 |
| `ProcessManager` | 统一子进程生命周期管理 |

---

## v0.4.0 — 实时数据开发（预计 6-8 周）

### 目标

覆盖实时数据链路开发场景。

### 功能规划

| 功能 | 优先级 | 说明 |
|-----|-------|------|
| **Flink CDC 配置生成** | P0 | 自动生成 MySQL/PostgreSQL CDC 同步作业 |
| **Kafka 连接器模板** | P0 | Flink Kafka Source/Sink 配置模板 |
| **实时 ETL 模板** | P0 | 常用实时转换逻辑（去重、窗口聚合、维表关联） |
| **CDC Schema 推断** | P1 | 从源表 DDL 自动推断目标表 Schema |
| **数据一致性检查** | P1 | 实时链路的数据对账 SQL |

### 知识库扩展

- Flink CDC 最佳实践
- Kafka 分区策略模板
- 实时数仓分层规范

---

## v0.5.0 — 数据质量与治理（预计 4-6 周）

### 目标

内置数据质量能力，从"生成代码"升级为"保证质量"。

### 功能规划

| 功能 | 优先级 | 说明 |
|-----|-------|------|
| **数据质量规则生成** | P0 | 自动生成空值/唯一性/枚举/范围校验 SQL |
| **质量报告模板** | P0 | 标准化数据质量报告输出 |
| **Schema 演化处理** | P0 | Iceberg ADD COLUMN / 类型变更的自动处理 |
| **数据血缘记录** | P1 | 记录表之间的依赖关系 |
| **数据分类标记** | P1 | 自动为表添加公开/内部/敏感/机密标记 |

### 新增工具

| 工具 | 说明 |
|-----|------|
| `QualityGenerator` | 质量校验规则生成 |
| `SchemaEvolver` | Schema 变更处理 |
| `DataLineage` | 血缘关系追踪 |

---

## v1.0.0 — 生产就绪（预计 8-10 周）

### 目标

达到可日常使用的稳定版本。

### 功能规划

| 功能 | 优先级 | 说明 |
|-----|-------|------|
| **Prompt Caching 优化** | P0 | 降低 token 消耗 50%+ |
| **知识库向量检索** | P0 | 替换文本匹配为语义检索，提升模板匹配准确率 |
| **多模态数据管理** | P0 | 多模态文件注册到 Iceberg 目录表，元数据管理 |
| **链路数据处理** | P0 | Jaeger/OpenTelemetry 格式解析，Trace 表建模 |
| **MCP 生态扩展** | P0 | Hive Metastore / Atlas / ClickHouse 内置 Server |
| **Airflow DAG 生成** | P0 | 生成可部署的 Airflow DAG 代码 |
| **Superset 配置生成** | P1 | 生成数据集 + 图表 + 看板配置 |
| **数据血缘可视化** | P1 | 基于 Atlas 的表级血缘图 |
| **多环境支持** | P1 | dev/staging/prod 环境配置切换 |
| **增量上下文管理** | P2 | 智能对话历史压缩和摘要 |

### 稳定性目标

| 指标 | 目标 |
|-----|------|
| SQL 生成成功率 | > 90% |
| 任务完成轮次 | ≤ 3 轮 |
| 用户纠正率 | < 15% |
| 测试覆盖率 | > 85% |
| 构建 + 测试时间 | < 30s |

---

## v1.1.0 — 报表与 BI 集成（预计 4-6 周）

### 功能规划

| 功能 | 说明 |
|-----|------|
| **Superset 部署** | 自动创建数据集、图表、看板 |
| **FineBI 数据包** | 生成 FineBI 数据包配置 |
| **QuickBI 对接** | QuickBI 数据源和报表配置 |
| **DataWind 集成** | DataWind 数据准备和报表 |
| **报表模板市场** | 常用报表模板可复用 |

---

## v1.2.0 — 自观测与自动优化（预计 6-8 周）

### 目标

实现"Agent 用自身数据驱动自我进化"的闭环。

### 功能规划

| 功能 | 说明 |
|-----|------|
| **自观测数据分析** | 用 Spark SQL 分析 Agent 使用数据 |
| **错误模式自动识别** | 高频失败模式自动标记 |
| **模板质量评分** | 根据执行成功率给 SQL 模板打分 |
| **自动知识库优化** | 成功 SQL 模式自动加入知识库（需用户确认） |
| **Planner 策略调优** | 根据历史成功率调整步骤规划策略 |
| **/stats 增强看板** | 更丰富的可视化指标 |

---

## v1.3.0 — 多模态与向量检索（预计 6-8 周）

### 功能规划

| 功能 | 说明 |
|-----|------|
| **OCR 集成** | 图片/扫描件中的表格数据识别 |
| **ASR 集成** | 音频日志转文字 |
| **Embedding 管理** | 文本/图片 Embedding 生成和存储 |
| **向量检索** | 基于语义的多模态数据检索 |
| **向量表管理** | Milvus/Qdrant 向量库建表和管理 |

---

## v2.0.0 — 团队协作与分布式（预计 12+ 周）

### 功能规划

| 功能 | 说明 |
|-----|------|
| **团队知识库** | 团队共享 SQL 模板和最佳实践 |
| **代码版本管理** | 生成的代码自动 git 提交 |
| **代码评审集成** | 生成代码后自动创建 PR |
| **多 Agent 协作** | 多个 Agent 分工处理大型任务 |
| **权限管理** | 数据源访问权限控制 |
| **实时自优化** | 基于团队使用数据全局优化 |

---

## 技术演进路线

```
v0.1  CLI + LLM + 基础工具
v0.2  + 稳定性优化 + Prompt Caching
v0.3  + 直接执行（Spark/Flink/ClickHouse）
v0.4  + 实时数据开发（CDC + Kafka）
v0.5  + 数据质量 + Schema 演化
v1.0  + 知识库向量化 + 多模态 + MCP 生态
v1.1  + BI 集成（Superset/FineBI）
v1.2  + 自观测 + 自动优化
v1.3  + OCR/ASR + 向量检索
v2.0  + 团队协作 + 多 Agent
```

---

## 核心原则

1. **代码优先，执行由用户掌控** — Agent 生成代码，用户决定何时/何地执行
2. **渐进式增强** — 每个版本都有可用的核心价值
3. **数据驱动迭代** — 通过自观测数据指导优化方向
4. **安全内建** — 每个版本的安全能力持续加强
5. **用户可控** — 知识库、模板、配置完全开放，用户可以自定义
